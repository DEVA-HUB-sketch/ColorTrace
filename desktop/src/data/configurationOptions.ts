import type { ConfigurationOption } from '@/types/configuration';

/**
 * Authoritative test configurations will be provided by the backend.
 * Currently empty until the backend configuration service is connected.
 */
export const frontendConfigurationPresets: ConfigurationOption[] = [];
